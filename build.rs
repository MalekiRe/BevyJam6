use std::env;

fn main() {
   /* dotenv::dotenv().ok();
    println!("cargo:rustc-env=LEADERBOARD_ONE={}", env::var("LEADERBOARD_ONE").unwrap());
    println!("cargo:rustc-env=LEADERBOARD_TWO={}", env::var("LEADERBOARD_TWO").unwrap());*/
}